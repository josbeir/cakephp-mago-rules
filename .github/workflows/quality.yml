name: Quality

on:
  push:
  pull_request:
  schedule:
    - cron: '0 6 * * 1'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  package:
    name: Package (PHP ${{ matrix.php }})
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        php: ['8.4', '8.5']

    steps:
      - uses: actions/checkout@v4

      - uses: shivammathur/setup-php@v2
        with:
          php-version: ${{ matrix.php }}
          coverage: none

      - name: Cache Composer downloads
        uses: actions/cache@v4
        with:
          path: ~/.cache/composer/files
          key: composer-${{ runner.os }}-php-${{ matrix.php }}-${{ hashFiles('composer.json') }}
          restore-keys: composer-${{ runner.os }}-php-${{ matrix.php }}-

      - name: Install dependencies
        run: composer update --no-interaction --prefer-dist --prefer-stable

      - name: Validate Composer metadata
        run: composer validate --no-check-publish

      - name: Check formatting and linting
        run: composer run cs-check

      - name: Run unit tests
        run: composer test

      - name: Validate extension registration and corpus expectations
        run: |
          composer run validate-extension
          composer run lint-corpus

      - name: Test installed consumer configuration
        run: |
          composer run test-consumer
          composer run test-fixes

  cakephp-6:
    name: CakePHP 6.x compatibility
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - uses: shivammathur/setup-php@v2
        with:
          php-version: '8.4'
          coverage: none

      - name: Cache Composer downloads
        uses: actions/cache@v4
        with:
          path: ~/.cache/composer/files
          key: composer-${{ runner.os }}-php-8.4-${{ hashFiles('composer.json') }}
          restore-keys: composer-${{ runner.os }}-php-8.4-

      - name: Install dependencies
        run: composer update --no-interaction --prefer-dist --prefer-stable

      - name: Check CakePHP 6.x compatibility
        run: scripts/check-cakephp-6.sh
